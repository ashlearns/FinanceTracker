// An enum (short for enumeration) is a special "value type" that lets you define a set of named constants.
public enum TransactionSortField
{
  None,

    // Sort transactions based on the title 
    Title,

    // Sort transactions based on the amount
    Amount,

    // Sort transactions based on the date (ascending/descending)
    Date
}
